Simple project to demonstrate akka usage. It generates test file ("id;number") and later reads that file and aggregates number's sum per specific id.
Result is generated into separate file (id => sum)
